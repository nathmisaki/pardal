%h2 Registro de usuário

- semantic_form_for resource_name, resource, :url => registration_path(resource_name) do |f|
  = f.error_messages
  - f.inputs do
    = f.input :email
    = f.input :password
    = f.input :password_confirmation

  - f.buttons do
    = f.commit_button "Registrar-se"

= render :partial => "shared/devise_links"
